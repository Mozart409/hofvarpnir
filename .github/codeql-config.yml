name: "CodeQL Config"

query-filters:
  # Ignore hard-coded cryptographic values in test code
  # These are intentional test fixtures, not real credentials
  - exclude:
      id: rust/hard-coded-cryptographic-value
      tags contain: /test/
