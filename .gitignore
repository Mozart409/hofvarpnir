result
target/
.direnv/

.env

# SQLx - commit the workspace-wide offline query metadata at the repo root
# (generated by `just prepare`, i.e. `cargo sqlx prepare --workspace`).
# Ignore stray per-crate .sqlx/ dirs so only the root one is tracked.
crates/*/.sqlx/

.claude/
/result-ffmpeg-bin
/result-ffmpeg-man
/result-ytdlp
